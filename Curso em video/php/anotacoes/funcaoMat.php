<?php 
    $result = abs(-500); //ABS retira o sinal do numero

    $binarioParaDecimal = base_convert(1100 , 2, 10); //Converte bases numéricas

    $divisaoInteira = intdiv(5 , 2); //Isso faz a divisão SEM VIRGULA entre números

    $maiorNumero = max(7.5,2,8,9,10); //Vê o maior número nos itens mostrados

    $menorNumero = min(1,5,7,10,64); //Vê o menor número

    $potencia = pow(10, 2); //Faz a potenciação

    $raizQuadrada = sqrt(81); //Vê a raiz quadrada de um número
?>